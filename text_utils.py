"""
╔══════════════════════════════════════════════════════════════════════════════╗
║                              TEXT UTILS                                      ║
║          Нормализация текста: цифры, английские буквы, названия             ║
╚══════════════════════════════════════════════════════════════════════════════╝
"""

import re

# ═══════════════════════════════════════════════════════════════════════════════
# ЗАМЕНА АНГЛИЙСКИХ НАЗВАНИЙ (машины, бренды, сервисы)
# ═══════════════════════════════════════════════════════════════════════════════

CAR_NAMES = {
    "GT-R": "джи ти ар",
    "Nissan": "ниссан",
    "Skyline": "скайлайн",
    "GT": "джи ти",
    "RB26": "ар би двадцать шесть",
    "HKS": "эйч кей эс",
    "OnlyFans": "онли фанс",
    "Patreon": "патреон",
    "YouTube": "ютуб",
    "Instagram": "инстаграм",
    "TikTok": "тикток",
    "Discord": "дискорд",
    "Telegram": "телеграм",
}

# ═══════════════════════════════════════════════════════════════════════════════
# ЗАМЕНА АНГЛИЙСКИХ БУКВ НА РУССКИЕ ПРОИЗНОШЕНИЯ
# ═══════════════════════════════════════════════════════════════════════════════

ENG_TO_RUS = {
    'a': 'эй', 'b': 'би', 'c': 'си', 'd': 'ди', 'e': 'и',
    'f': 'эф', 'g': 'джи', 'h': 'эйч', 'i': 'ай', 'j': 'джей',
    'k': 'кей', 'l': 'эл', 'm': 'эм', 'n': 'эн', 'o': 'оу',
    'p': 'пи', 'q': 'кью', 'r': 'ар', 's': 'эс', 't': 'ти',
    'u': 'ю', 'v': 'ви', 'w': 'дабл-ю', 'x': 'экс', 'y': 'уай', 'z': 'зет'
}

# ═══════════════════════════════════════════════════════════════════════════════
# ЗАМЕНА ЦИФР НА СЛОВА
# ═══════════════════════════════════════════════════════════════════════════════

DIGIT_MAP = {
    '0': 'ноль', '1': 'один', '2': 'два', '3': 'три', '4': 'четыре',
    '5': 'пять', '6': 'шесть', '7': 'семь', '8': 'восемь', '9': 'девять'
}

NUMBER_WORDS = {
    "100": "сто", "200": "двести", "300": "триста", "400": "четыреста",
    "500": "пятьсот", "600": "шестьсот", "700": "семьсот", "800": "восемьсот",
    "900": "девятьсот", "1000": "тысяча", "2000": "две тысячи", "3000": "три тысячи",
    "164": "сто шестьдесят четыре", "68": "шестьдесят восемь"
}

# ═══════════════════════════════════════════════════════════════════════════════
# ФУНКЦИЯ ПРЕОБРАЗОВАНИЯ ЧИСЕЛ В СЛОВА
# ═══════════════════════════════════════════════════════════════════════════════

def convert_number_to_words(num):
    """Преобразует число в слова (поддержка до 9999)"""
    if num < 10:
        return ['ноль','один','два','три','четыре','пять','шесть','семь','восемь','девять'][num]
    elif num < 20:
        teens = ['десять','одиннадцать','двенадцать','тринадцать','четырнадцать','пятнадцать',
                 'шестнадцать','семнадцать','восемнадцать','девятнадцать']
        return teens[num - 10]
    elif num < 100:
        tens = ['','десять','двадцать','тридцать','сорок','пятьдесят','шестьдесят',
                'семьдесят','восемьдесят','девяносто']
        t = tens[num // 10]
        u = convert_number_to_words(num % 10) if num % 10 != 0 else ''
        return f"{t} {u}".strip()
    elif num < 1000:
        hundreds = ['','сто','двести','триста','четыреста','пятьсот','шестьсот','семьсот','восемьсот','девятьсот']
        h = hundreds[num // 100]
        rest = convert_number_to_words(num % 100) if num % 100 != 0 else ''
        return f"{h} {rest}".strip()
    elif num < 10000:
        thousands = ['','одна тысяча','две тысячи','три тысячи','четыре тысячи','пять тысяч','шесть тысяч','семь тысяч','восемь тысяч','девять тысяч']
        t = thousands[num // 1000]
        rest = convert_number_to_words(num % 1000) if num % 1000 != 0 else ''
        return f"{t} {rest}".strip()
    else:
        return str(num)

# ═══════════════════════════════════════════════════════════════════════════════
# ОСНОВНАЯ ФУНКЦИЯ НОРМАЛИЗАЦИИ
# ═══════════════════════════════════════════════════════════════════════════════

def normalize_text_for_tts(text):
    """Заменяет английские названия, цифры и буквы на русские"""
    
    # 1. Замена английских названий (машины, бренды)
    for eng, rus in CAR_NAMES.items():
        text = text.replace(eng, rus)
        text = text.replace(eng.lower(), rus)
    
    # 2. Замена частых чисел
    for num, word in NUMBER_WORDS.items():
        text = text.replace(num, word)
    
    # 3. Замена диапазонов (52-54 → пятьдесят два пятьдесят четыре)
    text = re.sub(r'(\d+)-(\d+)', lambda m: f"{convert_number_to_words(int(m.group(1)))} {convert_number_to_words(int(m.group(2)))}", text)
    
    # 4. Замена всех остальных чисел (от 10 до 9999)
    def replace_number(match):
        num_str = match.group(0)
        try:
            return convert_number_to_words(int(num_str))
        except:
            return num_str
    text = re.sub(r'\b\d{2,4}\b', replace_number, text)
    
    # 5. Замена отдельных цифр
    for digit, word in DIGIT_MAP.items():
        text = text.replace(digit, word)
    
    # 6. Замена английских букв на русские
    for eng, rus in ENG_TO_RUS.items():
        text = re.sub(r'\b' + eng + r'\b', rus, text, flags=re.IGNORECASE)
    
    return text

# ═══════════════════════════════════════════════════════════════════════════════
# ОЧИСТКА ТЕКСТА (удаление лишних символов)
# ═══════════════════════════════════════════════════════════════════════════════

def clean_text(text):
    """Очищает текст от лишних символов и нормализует числа"""
    text = re.sub(r'(\d+)\s*-\s*(\d+)', r'\1 \2', text)
    return text